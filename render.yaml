services:
  - type: web
    name: marigold-disease-backend
    env: node
    plan: free
    root: backend
    buildCommand: npm install
    startCommand: npm run start
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      - key: SKIP_ML
        value: "true"
